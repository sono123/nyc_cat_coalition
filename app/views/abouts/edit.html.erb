<h1>About#edit</h1>
<p>Find me in app/views/about/edit.html.erb</p>


<%= form_for @about do |f| %>

  <% if @about.errors.any? %>

    <div id="error_explanation">
      <h2><%= pluralize(@about.errors.count, "error") %> prohibited About from being saved:</h2>
      <ul>

        <% @about.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>

      </ul>
    </div>

  <% end %>

    <%= f.label :header %>
    <%= f.text_field :header %>
    <br>

    <%= f.label :body %>
    <%= f.text_area :body %>
    <br>

    <%= f.submit %>

<% end %>






